<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soloforge.board.task.mapper.TaskMapper">

  <resultMap id="taskDetailMap" type="com.soloforge.board.task.entity.TaskDetail">
    <id column="id" property="id"/>
    <result column="board_id" property="boardId"/>
    <result column="column_id" property="columnId"/>
    <result column="title" property="title"/>
    <result column="assignee_id" property="assigneeId"/>
    <result column="priority" property="priority"/>
    <result column="due_date" property="dueDate"/>
    <result column="description" property="description"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
    <result column="assignee_name" property="assigneeName"/>
    <result column="column_code" property="columnCode"/>
  </resultMap>

  <sql id="detailColumns">
    t.id,
    t.board_id,
    t.column_id,
    t.title,
    t.assignee_id,
    t.priority,
    t.due_date,
    t.description,
    t.created_at,
    t.updated_at,
    u.display_name AS assignee_name,
    c.code AS column_code
  </sql>

  <sql id="detailFrom">
    FROM task t
    JOIN app_user u ON u.id = t.assignee_id
    JOIN board_column c ON c.id = t.column_id
  </sql>

  <select id="findDetailById" resultMap="taskDetailMap">
    SELECT <include refid="detailColumns"/>
    <include refid="detailFrom"/>
    WHERE t.id = #{id}
  </select>

  <select id="findByCondition" parameterType="com.soloforge.board.query.dto.TaskQuery" resultMap="taskDetailMap">
    SELECT <include refid="detailColumns"/>
    <include refid="detailFrom"/>
    <where>
      <if test="keyword != null and keyword != ''">
        AND (
          t.title LIKE CONCAT('%', #{keyword}, '%')
          OR t.description LIKE CONCAT('%', #{keyword}, '%')
        )
      </if>
      <if test="assigneeId != null">
        AND t.assignee_id = #{assigneeId}
      </if>
      <if test="priority != null and priority != ''">
        AND t.priority = #{priority}
      </if>
      <if test="dueFilter == 'OVERDUE'">
        AND t.due_date IS NOT NULL
        AND t.due_date &lt; #{referenceDate}
      </if>
      <if test="dueFilter == 'DUE_SOON'">
        AND t.due_date IS NOT NULL
        AND t.due_date &gt;= #{referenceDate}
        AND t.due_date &lt; DATEADD(DAY, 2, CAST(#{referenceDate} AS DATE))
      </if>
    </where>
    ORDER BY t.created_at DESC, t.id DESC
  </select>

  <select id="findReminderTasks" resultMap="taskDetailMap">
    SELECT <include refid="detailColumns"/>
    <include refid="detailFrom"/>
    WHERE t.assignee_id = #{assigneeId}
      AND t.due_date IS NOT NULL
    ORDER BY t.due_date ASC, t.id ASC
  </select>

  <insert id="insert" parameterType="com.soloforge.board.task.entity.Task" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO task (board_id, column_id, title, assignee_id, priority, due_date, description, created_at, updated_at)
    VALUES (#{boardId}, #{columnId}, #{title}, #{assigneeId}, #{priority}, #{dueDate}, #{description},
            CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
  </insert>

  <update id="update" parameterType="com.soloforge.board.task.entity.Task">
    UPDATE task
    SET title = #{title},
        column_id = #{columnId},
        assignee_id = #{assigneeId},
        priority = #{priority},
        due_date = #{dueDate},
        description = #{description},
        updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id}
  </update>

  <update id="updateColumnId">
    UPDATE task
    SET column_id = #{columnId},
        updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id}
  </update>

  <delete id="deleteById">
    DELETE FROM task WHERE id = #{id}
  </delete>

</mapper>
