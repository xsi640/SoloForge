# 子代理任务书模板

> 用法：复制本模板填写后，作为单个并行子代理的任务输入。一个模板对应一个代理、一个互斥的写入范围。

## 角色与范围

你是 <项目名> 的并行开发 Agent <编号>，负责 <TASK-### 列表 / 模块名>。

- 工作目录：<绝对路径>
- 工程根：<绝对路径>
- 写入白名单（只允许在以下路径创建/修改文件）：
  - <路径一>
  - <路径二>
- 严禁修改：<冻结的公共层路径>、<其他模块路径>、<构建配置>、<入口类>、<schema>
  - 这些是其他代理或已冻结的文件。若你认为契约有问题，**不要改**，在最终报告中说明。

## 必读

- <阶段产物路径> 的 <具体章节>（例如接口契约、状态约定、视觉规范）
- <任务文档路径> 中 `TASK-###` 的验收标准
- 可复用的既有资产：<设计系统样式 / 工具函数 / 既有实现模式>

## 已冻结的契约（必须严格按签名实现，不要改动）

- <实体 / DTO / VO / 枚举 清单>
- <Mapper 接口及其方法签名>
- <Service 接口及其方法签名>
- 跨模块可用接口（由其他代理实现，直接注入使用，不要自己实现）：
  - <接口.方法()>
- 错误处理方式：业务失败请 throw <统一异常>(<错误码>, "可展示文案")，由全局异常处理器统一转换，不要自己拼响应体。

## 需要创建的文件

1. <文件路径与职责>
2. <文件路径与职责>

## 关键行为要求

<逐条写清业务规则、边界条件、排序、校验、错误码与 HTTP 状态的对应关系>

## 质量要求

- 分层严格遵守：<controller 只依赖 service / service 承担业务规则与事务 / SQL 全部写在 XML 中>
- 数据库实体不作为接口出入参，必须转换为 VO/DTO
- 不使用 <被排除的框架>，不写行内注释（除非必要）
- **范围外负面清单**（明确不要做的事）：<列出需求/架构阶段已排除的能力>
- 文本编码 UTF-8 无 BOM

## 自检

完成后运行 <编译命令> 自检。若报错只出自你自己写入范围内的文件，请修好；若报错来自其他代理尚未完成的类，请在报告中说明并忽略。

## 最终回答（必须包含）

1. 创建/修改的**全部文件路径**；
2. 逐条说明 `TASK-###` 的**验收标准如何满足**；
3. 自检结果（编译/构建/测试命令与输出）；
4. **明确区分**「已实跑验证」与「未验证」的部分；
5. 你对契约的疑问或发现的跨模块问题（如有）。